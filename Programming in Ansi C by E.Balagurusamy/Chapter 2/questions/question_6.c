/*
1.6 Leap Year Check

Rule
Divisible by 4 → leap year
BUT if divisible by 100 → not leap
BUT if divisible by 400 → leap

Algorithm
Start
Input year
If (year % 400 == 0) → leap
Else if (year % 100 == 0) → not leap
Else if (year % 4 == 0) → leap
Else → not leap
Display result
Stop

*/
